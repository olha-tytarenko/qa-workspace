# Copy to `.env.local` for local overrides. Never commit real environment files.
# Base URL of the backend API, without the `/api` prefix.
# Under `docker compose`, compose.yaml sets this for you.
VITE_API_URL=http://localhost:8000
