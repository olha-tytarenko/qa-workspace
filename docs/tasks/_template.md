# Task: <short title>

Copy this file to `docs/tasks/<short-name>.md` (or paste it into the request). Keep each section short. Write "none" rather than deleting a section.

## Goal

What problem this task solves and for whom.

## User-visible outcome

What a user can do or see when the task is done.

## Scope

What is included.

## Explicit non-goals

What is not included, even if it seems related.

## Relevant accepted decisions

Links to the parts of `docs/decisions.md` that apply. Note any decision that must change.

## API or data-model impact

Endpoints, schemas, tables, migrations, contracts. "None" if there is no impact.

## Authorization rules

Who may do what, including the non-member (`404`) case. "None" if not applicable.

## Acceptance criteria

Observable, verifiable conditions.

## Required tests

Test layer and scenarios: success, validation, permission, isolation, failure.

## Required verification

Exact commands that must pass (see the Definition of Done in `CLAUDE.md`).

## Assumptions and unresolved questions

Assumptions the implementer may make, and open questions that must be answered before starting.
